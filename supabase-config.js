// Neural-iA — configuração pública do cliente Supabase.
// A URL e a anon/publishable key podem existir no frontend; NUNCA coloque service_role ou secrets de APIs aqui.
window.NEURAL_CONFIG = Object.freeze({
  supabaseUrl: "https://avqvecghixhrpqxucubn.supabase.co",
  // Preencha com a Publishable key (ou anon key legada) do projeto no Dashboard > Settings > API.
  supabaseAnonKey: ""
});
